server {
    listen 8080;

    location = /health {
        add_header Content-Type text/plain;
        return 200 "healthy\n";
    }

    location / {
        add_header Content-Type text/plain;
        return 200 "web service\n";
    }
}
